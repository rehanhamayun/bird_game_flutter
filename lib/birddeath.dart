import 'package:flutter/material.dart';

class BirdDeath extends StatelessWidget {
  const BirdDeath({Key? key}) : super(key: key);

  // IN PROGRESS

  @override
  Widget build(BuildContext context) {
    return Scaffold();
  }
}
