import 'package:flutter/material.dart';

class Barriers extends StatelessWidget {
  const Barriers({Key? key}) : super(key: key);

  // IN PROGRESS

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
